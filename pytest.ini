[pytest]
markers =
    perf: performance tests (set PERF_TEST_ROWS to enable)
    integration: integration tests (slower, require filesystem setup)
    slow: tests that take more than 1 second
testpaths = tests
addopts = -v --tb=short
